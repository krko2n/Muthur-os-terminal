# Example Terminal Game

Optional example package for a future tiny terminal game.

This package intentionally contains only a manifest and placeholder entrypoint.
It is not registered in core and does not make games required for terminal-only
mode.

## Permission example

The manifest declares `system.info` as an example known permission. The
placeholder entrypoint does not use it yet.
