# src/cli

Terminal-only mode entry point and CLI utilities.

This directory is for code that runs in pure terminal mode
without the GUI. It must not import React or GUI components.

Terminal-only mode must remain fully functional without
the Tauri desktop shell or any network connection.
