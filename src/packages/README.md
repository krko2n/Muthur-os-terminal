# src/packages

Optional feature packages that extend MUTHUR functionality.

Each subdirectory is a self-contained module that can be
enabled or disabled without affecting core operation.
Packages must declare their dependencies explicitly.
